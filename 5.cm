/* 5.cm
   Invalid program.
   Tests semantic errors:
   1) redefinition of variable x
   2) use of undefined variable y
   3) call to undefined function foo
*/

int main(void)
{
    int x;
    int x;
    int z;

    z = y + 1;
    z = foo(3);

    return 0;
}