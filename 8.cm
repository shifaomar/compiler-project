/* 8.cm
   Valid program that should compile but trigger
   a runtime array bounds error.
   Tests global arrays and bounds checking.
*/

int a[3];

void main(void) {
  int i;

  i = 0;
  while (i < 3) {
    a[i] = i + 1;
    i = i + 1;
  }

  output(a[1]);
  output(a[3]);
}