/* 2.cm
   Valid program.
   Tests self-recursion, function calls, if-else, and return values.
*/

int fact(int n) {
  if (n < 2) return 1;
  else return n * fact(n - 1);
}

void main(void) {
  int x;
  int y;

  x = 5;
  y = fact(x);

  output(y);
}