/* 4.cm
   Invalid program.
   Tests syntax errors:
   1) missing semicolon after declaration
   2) missing right parenthesis in if condition
   3) malformed assignment expression
*/

int main(void)
{
    int x
    x = 5;

    if (x < 10
        write x;

    x = + ;

    return 0;
}